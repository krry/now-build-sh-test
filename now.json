{
  "version": 2,
  "name": "now-build-sh-test",
  "build": {
    "env": {
      "GIT_SUBMODULE_STRATEGY": "recursive"
    }
  },
  "builds": [
    { "src": "./build.sh",
      "use": "@now/static-build",
      "config": { "distDir": "./public" }}
  ]
}
